<% if @contact.destroyed? %>
  $('#contact-' +<%= @contact.id %>).fadeOut(600);
  toastr.success("Contact deleted successfully");
<% else %>
  toastr.error("There was a problem deleting the contact. Please try again.");
<% end %>