<% if @contact.destroyed? %>
  $('#contact-' +<%= @contact.id %>).fadeOut(600);
  toastr.info("Contact deleted successfully");
<% else %>
  toastr.info("There was a problem deleting the contact. Please try again.");
<% end %>