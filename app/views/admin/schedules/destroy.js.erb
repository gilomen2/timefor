<% if @schedule.destroyed? %>
  $('#schedule-' +<%= @schedule.id %>).fadeOut(600);
  toastr.success("Schedule deleted successfully");
<% else %>
  toastr.error("There was a problem deleting the schedule. Please try again.");
<% end %>
